# linear-spec テンプレート

見出しは変えない（レビュアーと後工程が見出しで場所を探す）。該当しない節は「なし」と書いて残す。

---

## requirements

```markdown
# 要件定義 — <ISSUE> <タイトル>

## 背景と目的
<誰が・何に困っていて・何ができればよいか。2〜4文>

## 要求 → 要件
| 要求 | 要件ID | 要件（システムが満たすこと） | 実現性 | 根拠 |
|---|---|---|---|---|
| R1 <issue の言葉> | FR-1 | <振る舞い> | 可／条件付き／不可 | `src/...:123` |
| R2 ... | 対象外 | <理由> | — | — |

## 機能要件
### FR-1 <名前>
- 受け入れ条件
  - Given <前提> / When <操作> / Then <観察できる結果>
  - Given ... （空・エラー・ゲスト・無料会員・モバイルの場合）

## 非機能要件
<性能・オフライン・アクセシビリティ・プライバシー（公開されるか）など。無ければ「なし」>

## 後方互換性チェック（CLAUDE.md の各項目）
- スキーマ：<変更の有無と、旧レコードの扱い>
- 旧形式の読み出し：
- 表示先の維持：
- 部分更新で他の欄を落とさない：
- 削除は明示操作だけ：
- 配信契約（publish・同期 API・MCP）：

## 仮定と未決事項
- 仮定：<issue に書かれていないが置いた前提>
- 未決：<ユーザー判断が要るもの>

## 対象外
```

---

## design

```markdown
# 設計 — <ISSUE> <タイトル>

プロトタイプ: <Artifact URL>

## 方針
<どう実現するかを3〜5行。捨てた代替案があれば1行ずつ理由>

## 変更ファイル
| 種別 | パス | 責務 / 変更内容 | 要件ID |
|---|---|---|---|
| 変更 | `src/core/...` | | FR-1 |
| 新規 | `src/ui/components/...` | | FR-1 |

## データモデル
<Zod スキーマ差分（型の形で）。旧レコードをどう読むか。同期・バックアップ・publish・MCP への波及>

## UI
### 画面: <画面名>（ルート `#/...`）
- 現状: `screens/<name>-desktop.png`
- 変更点: ①… ②…（番号はプロトタイプと対応）
- 状態: 空 / 読み込み中 / エラー / オフライン / ゲスト / 無料会員
- モバイル: 
- 使う共通部品: `<Component>`（CODEMAP 共通部品カタログ）
- 文言: <主要なボタン・見出し・トースト>

## ロジック・API
<core の関数シグネチャ、store の action、functions/ のエンドポイント。無ければ「なし」>

## テスト計画
| 層 | 何を固定するか | 置き場所 |
|---|---|---|
| unit | | `src/core/...test.ts` |
| integration | | |
| e2e | | `e2e/...` |

## リスク・未決事項
```

---

## spec（最終仕様書・簡潔版）

```markdown
# <タイトル> 仕様書（<ISSUE>）

- 要件: <サブ issue ID> ／ 設計: <サブ issue ID> ／ プロトタイプ: <URL>
- 状態: レビュー通過（Round n）

## 何を作るか
<3行以内>

## 振る舞い
<要件IDごとに、受け入れ条件を箇条書きで。Given/When/Then は短く>

## 画面
<画面ごとに変更点を箇条書き。スクショとプロトタイプの参照>

## 実装の骨格
<変更ファイルと責務の表（design.md から最小限）。データモデル差分>

## テスト
<層ごとに1〜3行>

## 守ること
<後方互換性で効いてくる項目だけ>

## やらないこと
```

---

## impl-prompt（実装依頼プロンプト）

```markdown
Linear <ISSUE>「<タイトル>」を実装してください。

## 読むもの（この順で）
1. `docs/specs/<ISSUE>/spec.md` — 仕様（正本）
2. `docs/specs/<ISSUE>/design.md` — 変更ファイルとテスト計画の詳細
3. `docs/specs/<ISSUE>/prototype.html` と `screens/` — 画面の完成イメージ（<Artifact URL>）
4. `docs/CODEMAP.md` — 該当節のみ

## 作業範囲
- 変更するファイル: <一覧>
- 変更しないもの: <明示>

## 進め方
1. <テストから書くもの>
2. <実装順>
3. 画面の文言は toc-copy スキルで整える
4. 完了後 `/codemap-update` を実行

## 完了条件
- [ ] FR-1 … の受け入れ条件がテストで固定されている
- [ ] `pnpm typecheck` / `pnpm test` / `pnpm lint` が通る
- [ ] <e2e があれば>
- [ ] プロトタイプと見比べて差分がない（desktop / mobile）

## 守る規約
- `src/core/` は React と `src/ui/` を import しない
- スキーマは optional で足す・既存欄を消さない（CLAUDE.md「後方互換性」）
- <この変更で特に効くもの>

## やらないこと
- <スコープ外>
```
