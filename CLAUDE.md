# novel-studio

ローカルファーストの小説執筆ツール（Vite + React 19 + Cloudflare Pages Functions）。

## コード調査

**コードを調べる前に `docs/CODEMAP.md` を読む。** リポジトリ全体の地図で、
「何をどこで直すか」がそこに索引化してある。全文検索やディレクトリ探索より先に使う。
**UI 部品・フック・ヘルパを新規に作る前も**、同ファイルの「共通部品カタログ」で在庫を確認する。

## 実装・修正のあと

**`/codemap-update` スキルを実行して `docs/CODEMAP.md` を追従させる。**
ファイル構成・export・API・ルート・マイグレーション・コマンドのいずれかが変わったら必須。
中身だけの変更（バグ修正・スタイル）なら、スキルが「更新不要」と判断して終わる。

## 破ってはいけない規約

- `src/core/` は `src/ui/` と React を import しない（純TS・テストの主戦場）
- 状態管理ライブラリ（Redux/Zustand）は使わない — 自前 `useSyncExternalStore` ストア
- `src/ui/components/ui/` は shadcn/ui のコピー品。lint 対象外で、手を入れない
- 新しい依存を足す前に「コピーして所有できないか」を検討する

## 後方互換性（本番に利用者がいる）

利用者のローカルデータ（IndexedDB）・クラウドバックアップ・同期スナップショットには、
**過去の全バージョンで書かれたレコードがそのまま残っている**。改修で顧客のデータを
「消す・非表示にする・壊す」ことは絶対に避ける。具体的には：

- **スキーマからフィールドを消さない・必須化しない。** 保存物は読み書きとも
  `WorkSchema.parse` 等の Zod 検証を通る。旧レコードを弾く変更は、その1件どころか
  一覧ごと読めなくして「全作品が消えた」ように見える。廃止したい欄は optional のまま
  deprecated と注記して残す（例：用語集の `body`）。後から足す欄は
  `.optional()`（配列なら `.optional().default([])`）にする（例：Plot の `world`/`secrets`）。
- **旧形式の読み出しは互換ヘルパで吸収する**（例：`publicTextOf` が summary＋body を結合）。
  一括変換のマイグレーションより「保存時に新形式へ寄せる」遅延移行を選ぶ。
- **欄の出力先（表示場所）を無くさない。** UI から表示箇所が消えると、データが残っていても
  利用者には消えたのと同じ。欄を統合・移動するときは、旧データの中身が新しい表示に
  必ず合流することをテストで固定する。
- **1欄だけの更新で他の欄を落とさない。** patch を組むとき、フォームやツール入力に無い
  フィールド（サムネ・作者メモ等）は現在値で埋めるか据え置く（MCP の upsert は
  「渡した項目だけ書き換える」パッチ方式。省略＝据え置き、空文字＝削除）。
- **削除は明示操作だけ。** 完全削除はゴミ箱からの purge（と30日期限）に限る。
  新しいコードパスが暗黙にレコードを消していないか差分を疑う。
- **配信契約（publish バンドル・同期 API）を狭めない。** スキーマ変更時は旧クライアント／
  旧サーバとの送受両方向を考える（新しすぎるバンドルは 409 で弾かれる契約を守る）。
- 破壊的変更がどうしても要るときは、旧形式フィクスチャの回帰テストとマイグレーションを
  必ず書き、リリースノートに退避手段（バックアップ）を添える。

## コマンド

`pnpm test` / `pnpm typecheck` / `pnpm lint` / `pnpm build` / `pnpm test:e2e`
（CI はこの順に全部通す。push 前に最低限 typecheck と test）
