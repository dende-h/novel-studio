node_modules
dist
dist-ssr
coverage
*.local
.DS_Store
.vite
dev-dist

# TypeScript / ビルド成果物
*.tsbuildinfo

# mise（ローカル上書き・秘密値はコミットしない）
mise.local.toml
mise.*.local.toml

# Cloudflare Pages（ローカルの Functions シークレット）
.dev.vars
.dev.vars.*

# Cloudflare/wrangler のローカル状態（ローカル D1 sqlite にセッショントークン等が入る・ビルド中間物）
.wrangler/
.mf/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Playwright
test-results
playwright-report
.playwright
