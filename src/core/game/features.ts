/**
 * サウンドノベルの機能フラグ。
 *
 * まだ出さない機能を、**データの形は残したまま**（Cue の欄・目録の kind・プレイヤーの解釈・投稿契約）
 * 画面・MCP・書き出しから外すための 1 箇所。出すときはここを true に戻すだけで、
 * 保存済みの演出譜はそのまま読み書きできる（欄を消さない・必須化しない＝後方互換の規約どおり）。
 */
export interface GameFeatures {
  /**
   * 効果音（組み込みの合成 SE と、運営テンプレの音声ファイル）。
   *
   * 2026-09-02：合成音は聞いて品質が足りず、用意した mp3 は再配布不可だったため今回は見送り
   * （D-GAME-SE-HOLD）。false のあいだは、演出エディタの効果音の欄と環境音のレーン・MCP の
   * se / se_repeat・書き出しの `ses`・管理ページの効果音タブ・投稿とプレビューの音声取得が
   * すべて出ない。保存済みの `Cue.se` は消さず、書き出しでは黙って読み飛ばす。
   */
  readonly se: boolean
}

export const GAME_FEATURES: GameFeatures = { se: false }
